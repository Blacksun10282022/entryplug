{
  "name": "entryplug",
  "version": "0.1.0",
  "description": "Entry Plug: one read-only search (MCP), the sortie lock (PreToolUse hook), the compaction pin, the record reminder (Stop hook), and the boot panel on SessionStart. The content repo lives elsewhere; pre-commit and the deny rules are installed per content repo (see README).",
  "author": { "name": "Kyo" },
  "hooks": "./hooks/hooks.json",
  "mcpServers": "./.mcp.json"
}
