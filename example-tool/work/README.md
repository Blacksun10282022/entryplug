# work/

Products land here, one directory per equipment (`work/sunzi/`). Not indexed, not protected, freely writable:
a brief, a comparison table, a draft belongs here, never inside the equipment.

A product that also needs to live somewhere else gets copied there, with the master path noted in the copy.
