# workshop/

Where new equipment is built before it is registered in plug.yaml. Not indexed, not protected.

New equipment: build it in `workshop/<name>/` → give its SKILL.md the three header lines (Where / Needs Base /
Product copies) → move it to `tools/<name>/` and register it in plug.yaml. Registration is what makes it
protected; until then it is a draft like any other.
